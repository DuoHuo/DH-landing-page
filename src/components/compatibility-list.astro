---
import { Icon } from "astro-icon";
const { title, data, url, grid  } = Astro.props;
---

<div class="w-full max-w-6xl space-y-2">
  <div class="relative px-6 pt-8 pb-4 border bg-offset border-default">
    <h3
      class="absolute top-0 px-4 py-1 text-xs tracking-tight uppercase -translate-y-1/2 border border-current rounded-full right-4 bg-default"
    >
      {title}
    </h3>
    <ul class={grid}>
      {
        data.map(({ title, icon, url }) => (
          <li>
            <a class="flex flex-col items-center gap-2" href={url}>
              <Icon class="h-12" name={icon} />
              <span>{title}</span>
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</div>
